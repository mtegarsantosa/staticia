let fs,path;_1f1‍.x([["default",()=>objectResult]]);_1f1‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_1f1‍.w("path",[["default",["path"],function(v){path=v}]]);


const sorting = (obj) => {
  const operator = {
    ordered: {},
    run: (obj) => {
      Object.keys(obj).sort().forEach(function(key) {
        operator.ordered[key] = obj[key]
      })
      return operator
    }
  }
  return operator.run(obj)
}

               function objectResult(obj) {
  fs.writeFile(path.join(global.build_dir, 'data.js'), `var data = ${JSON.stringify(sorting(obj).ordered)}`, function (err) {
    if (err) throw err
  })
}
